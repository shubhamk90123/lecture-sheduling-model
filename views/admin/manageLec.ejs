<!DOCTYPE html>
<html lang="en">

  <head>
    <%- include('../partials/head') %>
  </head>

  <body>

    <%- include('../partials/sidebar') %>

    <div class="main">

      <%- include('../partials/topbar') %>

      <div class="container">

        <h2>All Assigned Lectures</h2>

        <table>
          <thead>
            <tr>
              <th>No</th>
              <th>Course Name</th>
              <th>Level</th>
              <th>Instructor</th>
              <th>Duration</th>
              <th>Date</th>
              <th>Actions</th>
            </tr>
          </thead>
          <tbody>
            <% if (!lectureData || lectureData.length === 0) { %>
              <tr><td colspan="7">No lectures scheduled yet</td></tr>
            <% } %>
            <% (lectureData || []).forEach((lec, idx) => { %>
              <tr>
                <td><%= idx + 1 %></td>
                <td><%= lec.courseName %></td>
                <td><%= lec.courseCode || lec.level %></td>
                <td><%= lec.instructor || lec.instructorName %></td>
                <td><%= lec.duration %></td>
                <td><%= lec.startDate || lec.date %></td>
                <td>
                  <button class="edit">Edit</button>
                  <button class="delete">Delete</button>
                </td>
              </tr>
            <% }); %>
          </tbody>
        </table>

      </div>

    </div>

  </body>

</html>